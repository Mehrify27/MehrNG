<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/item_bg"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="6dp"
    android:layout_marginBottom="6dp"
    android:background="@drawable/bg_item_normal"
    android:orientation="vertical">

    <LinearLayout
        android:id="@+id/info_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clickable="true"
        android:focusable="true"
        android:background="?attr/selectableItemBackground"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:padding="12dp">

        <!-- Unified Flag / Badge Card View on the Left -->
        <androidx.cardview.widget.CardView
            android:layout_width="40dp"
            android:layout_height="40dp"
            app:cardCornerRadius="6dp"
            app:cardElevation="0dp"
            app:cardBackgroundColor="@android:color/transparent">
            <!-- Protocol Badge Icon (Abbreviated or Flag Emoji) -->
            <TextView
                android:id="@+id/tv_type_badge"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@drawable/bg_protocol_badge"
                android:gravity="center"
                android:textColor="@color/app_text_primary"
                android:textSize="18sp"
                android:textStyle="bold"
                tools:text="🇩🇪" />
            <!-- Flag Image View -->
            <ImageView
                android:id="@+id/iv_flag"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:visibility="gone"
                tools:visibility="visible" />
        </androidx.cardview.widget.CardView>

        <!-- Info Description (Remarks and Address) -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="8dp"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tv_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/app_text_primary"
                android:textSize="15sp"
                android:textStyle="bold"
                android:maxLines="1"
                android:ellipsize="end"
                tools:text="MehrNetVPN (Telegram) #1" />

            <TextView
                android:id="@+id/tv_statistics"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:textColor="@color/apple_gray"
                android:textSize="12sp"
                android:maxLines="1"
                android:ellipsize="end"
                tools:text="212.102.53.195:443" />

        </LinearLayout>

        <!-- Latency Pill Badge -->
        <TextView
            android:id="@+id/tv_test_result"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_pill_green"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:paddingTop="5dp"
            android:paddingBottom="5dp"
            android:textColor="@color/apple_green"
            android:textSize="12sp"
            android:textStyle="bold"
            tools:text="140ms" />

    </LinearLayout>

    <!-- Hidden block for backward compatibility with existing view references -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone">
        <View android:id="@+id/layout_indicator" android:layout_width="0dp" android:layout_height="0dp" />
        <View android:id="@+id/layout_subscription" android:layout_width="0dp" android:layout_height="0dp" />
        <TextView android:id="@+id/tv_subscription" android:layout_width="0dp" android:layout_height="0dp" />
        <View android:id="@+id/layout_share" android:layout_width="0dp" android:layout_height="0dp" />
        <View android:id="@+id/layout_edit" android:layout_width="0dp" android:layout_height="0dp" />
        <View android:id="@+id/layout_remove" android:layout_width="0dp" android:layout_height="0dp" />
        <View android:id="@+id/layout_more" android:layout_width="0dp" android:layout_height="0dp" />
        <TextView android:id="@+id/tv_type" android:layout_width="0dp" android:layout_height="0dp" />
        <View android:id="@+id/card_flag" android:layout_width="0dp" android:layout_height="0dp" />
    </LinearLayout>

</LinearLayout>
